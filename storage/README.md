# Runtime Storage

This directory stores runtime files generated by CGAO.

Examples include:

- Login state
- Browser session
- Temporary cache
- SQLite database

These files are intentionally ignored by Git and should never be committed.

```
storage/
    state/
```

The `state` directory contains Playwright authentication files.

Never upload authentication files to GitHub.